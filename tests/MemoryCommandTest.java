package tests;

// import org.junit.Test;

public class MemoryCommandTest {
    // @Test
    // public void checkUids() {
    // }
    // @Test
    // public void TestCommandReciever() throws IOException {

    // // OutputStream output = System.out;
    // OutputStream output = new FileOutputStream("file.txt");
    // CommandInvoker invoker = new CommandInvoker(new ObjectOutputStream(output));

    // invoker.send(new WriteCommand(5, 10));
    // invoker.send(new ReadCommand(5));
    // invoker.send(new ExitCommand());

    // InputStream input = new FileInputStream("file.txt");
    // CommandReciever<Memory> receiver = new CommandReciever<>(new
    // Memory(System.out),
    // new ObjectInputStream(input));

    // receiver.handleCommand();

    // output.close();
    // input.close();
    // }
}
